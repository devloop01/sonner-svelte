// Max amount of toasts
export const TOAST_LIMIT = 10;

// Visible amount of toasts
export const VISIBLE_TOASTS_AMOUNT = 3;

// Viewport padding from sides (in px)
export const VIEWPORT_OFFSET = '32px';

// Default lifetime of a toasts (in ms)
export const TOAST_LIFETIME = 4000;

// Default toast width (in px)
export const TOAST_WIDTH = 356;

// Default gap between toasts (in px)
export const GAP = 14;

// Default swipe threshold (in px)
export const SWIPE_THRESHOLD = 20;

// Time before unmounting a toast (in ms)
export const TIME_BEFORE_UNMOUNT = 200;
